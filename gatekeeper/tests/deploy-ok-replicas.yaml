apiVersion: apps/v1
kind: Deployment
metadata:
  name: good-ok-replicas
  namespace: demo
spec:
  replicas: 3
  selector:
    matchLabels:
      app: good-ok-replicas
  template:
    metadata:
      labels:
        app: good-ok-replicas
    spec:
      containers:
      - name: nginx
        image: nginx:1.27.3
        securityContext:
          runAsUser: 1000
        resources:
          limits:
            cpu: 100m
            memory: 64Mi
