apiVersion: v1
kind: Pod
metadata:
  name: bad-hostnetwork
  namespace: demo
spec:
  hostNetwork: true
  containers:
  - name: nginx
    image: nginx:1.27.3
    securityContext:
      runAsUser: 1000
    resources:
      limits:
        cpu: 100m
        memory: 64Mi
